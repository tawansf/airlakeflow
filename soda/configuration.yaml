# Connection to the data warehouse for Soda scans.
# In production, prefer environment variables or secrets (e.g. SODA_POSTGRES_PASSWORD).
data_source postgres_datawarehouse:
  type: postgres
  connection:
    host: postgres
    port: 5432
    username: airflow
    password: airflow
    database: datawarehouse