# Minimal runtime for ETL (no Soda, no pytest in image). Add soda-* if you use quality checks.
apache-airflow>=2.10.0,<3
apache-airflow-providers-postgres>=5.0.0
pandas>=2.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
