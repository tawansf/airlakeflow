# Data contract - Silver Task (demo)
dataset: postgres_datawarehouse/datawarehouse/silver/task

checks:
  - row_count:
      name: Table is not empty
      threshold:
        must_be_greater_than: 0

columns:
  - name: title
    checks:
      - missing:
          name: Title never null
          threshold:
            must_be: 0
