# Data contract - Silver User (demo)
dataset: postgres_datawarehouse/datawarehouse/silver/user

checks:
  - row_count:
      name: Table is not empty
      threshold:
        must_be_greater_than: 0

columns:
  - name: name
    checks:
      - missing:
          name: Name never null
          threshold:
            must_be: 0
